/**
 * Para que possamos testar uma Calculadora
 * devemos ter a função construtora da mesma (de
 * modo que possamos intanciar a mesma) e então
 * exportamos a tal para aqueles que requirirem
 * este módulo.
 */
function Calc () {}

module.exports = Calc;
